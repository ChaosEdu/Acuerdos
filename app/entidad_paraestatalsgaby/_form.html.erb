<%= form_for (@entidad_paraestatal) do |f| %>
  <% if @entidad_paraestatal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@entidad_paraestatal.errors.count, "error") %> prohibited this entidad_paraestatal from being saved:</h2>

      <ul>
      <% @entidad_paraestatal.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

	<form>
		<fieldset>
			<legend>Nueva Entidad Paraestatal</legend>
			<div class="field">
			<strong><%= f.label :nom_entidad, "Nombre de entidad" %></strong><br />
			<%= f.text_field :nom_entidad , :placeholder=>"Entidad de la leche" %>
			</div>
			<div class="field">
				<strong><%= f.label :objeto_social %></strong><br />
				<%= f.text_area :objeto_social, :rows =>"4", :class=> "input-xxlarge" %>
			</div>
			  <div class="field">
				<strong><%= f.label :marco_normativo %></strong><br />
				<%= f.text_area :marco_normativo, :rows=> "4", :class=>"input-xxlarge" %>
			  </div>
			<div class="field">
			<strong><%= f.label :tipo_entidad_id, "Tipo de entidad:" %></strong><br />
			<%= f.select :tipo_entidad_id, TipoEntidad.all.collect {|p| [ p.nom_tipo_entidad, p.id ] }, { :include_blank => true } %>
		  </div>
  <div class="field">
    <strong><%= f.label :organo_de_gobierno_id, "Organo de gobierno que rige a la entidad " %></strong><br />
    <%= f.select :organo_de_gobierno_id, OrganoDeGobierno.all.collect {|p| [ p.nom_organo, p.id ] }, { :include_blank => true }  %>
  </div>
  <div class="field">
    <strong><%= f.label :secretaria_id, "Secretaría:" %></strong><br />
    <%= f.select :secretaria_id, Secretaria.all.collect {|p| [ p.nom_secretaria, p.id ] }, { :include_blank => true } %>
  </div>

  <div class="actions">
    <%= f.submit "#{ f.object.new_record? ? 'Crear entidad paraestatal': 'Actualizar entidad paraestatal'}", :class=> "btn" %>
  </div>

  <% end %>
  <%= link_to" ", :back, :class=>"icon-arrow-left"%>