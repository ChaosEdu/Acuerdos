<h1>Listado de emails</h1>

<table>
  <tr>
    <th>Correo</th>
    <th>Tipo email</th>
    <th>Empleado</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @emails.each do |email| %>
  <tr>
    <td><%= email.correo %></td>
    <td><%= email.tipo_email.try(:nom_tipo_email) %></td>
    <td><%= email.empleado.try(:nombre) %></td>
    <td><%= link_to 'Mostrar', email %></td>
    <td><%= link_to 'Editar', edit_email_path(email) %></td>
    <td><%= link_to 'Destruir', email, method: :delete, data: { confirm: 'Está seguro?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Nuevo email', new_email_path %>
