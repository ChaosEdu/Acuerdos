<%= form_for(@secretarium) do |f| %>
  <% if @secretarium.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@secretarium.errors.count, "error") %> prohibited this secretarium from being saved:</h2>

      <ul>
      <% @secretarium.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <strong><%= f.label :nom_secretaria, "Nombre de la secretaría" %></strong><br />
    <%= f.text_field :nom_secretaria %>
  </div>
  <div class="actions">
    <%= f.submit "#{ f.object.new_record? ? 'Crear secretaría': 'Actualizar secretaría'}" , :class => "btn" %>
  </div>
<% end %>
