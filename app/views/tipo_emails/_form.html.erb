<%= form_for(@tipo_email) do |f| %>
  <% if @tipo_email.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tipo_email.errors.count, "error") %> prohibited this tipo_email from being saved:</h2>

      <ul>
      <% @tipo_email.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nom_tipo_email, "Tipo de email" %><br />
    <%= f.text_field :nom_tipo_email %>
  </div>
  <div class="actions">
    <%= f.submit custom_submit(f.object, 'Crear tipo de email', 'Actualizar tipo de email') %>
  </div>
<% end %>
