<%= form_for(@tipo_tel) do |f| %>
  <% if @tipo_tel.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tipo_tel.errors.count, "error") %> prohibited this tipo_tel from being saved:</h2>

      <ul>
      <% @tipo_tel.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tipo_tel, "Tipo de teléfono" %><br />
    <%= f.text_field :nom_tipo_tel %>
  </div>
  <div class="actions">
    <%= f.submit "#{ f.object.new_record? ? 'Crear tipo de teléfono': 'Actualizar tipo de teléfono'}" %>
  </div>
<% end %>
