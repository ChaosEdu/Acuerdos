<h1>Listado de tipos de usuarios</h1>

<table>
  <tr>
    <th>Nombre de usuario</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @tipo_usuarios.each do |tipo_usuario| %>
  <tr>
    <td><%= tipo_usuario.nom_usuario %></td>
    <td><%= link_to 'Mostrar', tipo_usuario %></td>
    <td><%= link_to 'Editar', edit_tipo_usuario_path(tipo_usuario) %></td>
    <td><%= link_to 'Destruir', tipo_usuario, method: :delete, data: { confirm: 'Está seguro?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Nuevo tipo de usuario', new_tipo_usuario_path %>
